{
  "name": "nanoranch-site",
  "private": true,
  "description": "nano};{ranch — scalable stewardship for a range of rewilding",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
